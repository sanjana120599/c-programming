int main()
{
    int n1,n2,n3,max;
    printf("enter three digits");
    scanf("%d%d%d" ,&n1,&n2,&n3);
    max=n1;
    if (n2>max)
    max=n2;
    if (n3>max)
    max=n3;
    printf("largest number is %d",max);
    

    return 0;
}
